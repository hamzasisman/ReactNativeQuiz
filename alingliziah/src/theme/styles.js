import { colors } from './Colors';
import Fonts from './Fonts';

export default {
  colors,
  text: {
    // color: colors.BLACK,
    fontSize: Fonts.size(10)
    // fontFamily: Fonts.type.regular,
  }
};
